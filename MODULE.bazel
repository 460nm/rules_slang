module(
    name = "rules_slang",
    version = "0.1.0",
)

bazel_dep(name = "bazel_skylib", version = "1.8.2")

slang = use_extension("//slang:extensions.bzl", "slang")
use_repo(slang, "slang_toolchain")
