load("@rules_slang//slang:defs.bzl", "slang_shader", "slang_module")

slang_module(
    name = "mymod",
    srcs = [
        "mymod.slang",
    ],
)

slang_shader(
    name = "triangle",
    srcs = [
        "triangle.slang",
    ],
    deps = [
        ":mymod",
    ],
)
